package com.aadd.ydc.info.Interfaces;

public class MasUsadas {

	public static void main(String[] args) {
//////////////////////////////////////////////// Interfaces /////////////////////////////////////////////////////////////
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
		// Como, a diferencia de las herencias, una clase peude tener varias;
		// normalmente llenarremos nuestro proyecto de interfaces

		// ¿PORQUE?
		// Porque son muy útiles

		// Autocloseable (cierra bufferes etc en los bloques try catch al terminar)
		// Iterable (permite a una clase que contiene una lista iterarse y returnear lo
		// que su lista contiene)
		// Comparable (permite asignar un criterio para ordenar esos objetos)
		// Runnable (permite ejecutar un código en un hilo)
		// Serializable (permite serializar objetos para almacenarlos en archivos o
		// transmitirlos)
		// Cloneable (permite clonar un objeto usando el método super.clone() dentro de
		// la clase)
		// Stream (permite gestionar y utilizar los datos de objeto)

		// Usar interfaces es indispensable

	}

}
